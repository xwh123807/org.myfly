package org.myfly.platform.core.user.repository;

import org.myfly.platform.core.user.domain.SGlobalName;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SGlobalNameRepository extends JpaRepository<SGlobalName, String>{

}
